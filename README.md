# AIMALL — AI Execution Platform (redesign)

Static marketing site for AIMALL (爱莫科技). Plain HTML/CSS/JS, no build step.

## Run locally
```
python3 -m http.server 8000
```
then open http://localhost:8000

## Deploy
Served as a static site (e.g. GitHub Pages). All paths are relative.
